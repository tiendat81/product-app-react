import React from 'react';
import PropTypes from 'prop-types';
import { Box } from '@material-ui/core';

HomePage.propTypes = {};

function HomePage(props) {
  return <Box>Shop now</Box>;
}

export default HomePage;
